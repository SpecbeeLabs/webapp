
<div class="block block--{{ block_name }}" id="{{ block_id }}">
  {% if block_label %}
    <h2 class="block__title">{{ block_label }}</h2>
  {% endif %}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>